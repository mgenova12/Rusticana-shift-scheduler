
{
  "name": "rusticana-shift-scheduler",
  "engines": {
    "node": "8.9.4",
    "yarn": "0.27.5"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn add --dev gh-pages && yarn deploy"
  }
}
