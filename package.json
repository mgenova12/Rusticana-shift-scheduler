
{
  "name": "rusticana-shift-scheduler",
  "license": "MIT",
  "engines": {
    "node": "8.9.4",
    "yarn": "0.27.5"
  },
  "scripts": {
    "build": "yarn add --cwd client install && yarn add --cwd client build",
    "deploy": "cd ../../.. && pwd && cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}
