{
  "name": "rusticana-shift-scheduler",
  "license": "MIT",
  "engines": {
    "node": "8.9.4",
    "yarn": "0.27.5"
  },
  "scripts": {
    "build": "cd client && yarn add && yarn --cwd client build",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}